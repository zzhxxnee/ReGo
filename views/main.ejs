<!DOCTYPE html>

<html>
  <head>
    <meta charset="utf-8" />
    <title><%= title %></title>
    <!--css 들어갈 부분-->
  </head>

  <body>
    <h1>Main</h1>
    <p>
      <%= nickname %>

      <% 
      let count=0;
      let k=0;
      %>

      <a href="/logout">로그아웃</a>
      <% for (let i = goal_list.length - 1; i >= 0; i--) { %>
        
          <% if ( (arr[k]) == goal_list[i].goal_id) { %>
            
            <% k++ ; %>
            <b> <%= goal_list[i].goal_user %> : </b> <%= goal_list[i].goal_content %> <img src="/public/img/cheer_complete.png" width="15px">
            <br>
            <% }  else { %>

              <b> <%= goal_list[i].goal_user %> : </b> <%= goal_list[i].goal_content %>
              <form action="/cheer_goal" method="post" style="display: inline;">
                <input type="hidden" name="goal_id" value="<%=goal_list[i].goal_id%>">
                <input type="image" src="/public/img/cheer_up.png" width="15px" alt="제출버튼">
              </form>
              <br>
          
        <% } %>

          <br>
        </div>
      <!-- &emsp;&emsp; -->
      <% } %>
    </p>

      <!-- 새해 목표 작성하기 -->
      <form action="/write_goal" method="post">
        <label><%= nickname %> 의 한 줄 목표를 적어주세요.</label>
        <br>
        <input type="text" name="goal_content">
        <input type="submit" value="목표 올리기">
      </form>

  </body>
</html>